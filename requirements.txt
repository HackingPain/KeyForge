# Core web framework
# fastapi 0.110.1 hard-requires starlette>=0.37.2,<0.38.0. Newer starlette
# (0.40+) drops the on_startup kwarg fastapi calls internally, so the upper
# bound matters. Pin to the exact version fastapi ships with for CI/local parity.
fastapi==0.110.1
uvicorn>=0.24.0
starlette==0.37.2
pydantic>=2.5.0
python-multipart>=0.0.6

# Database
motor>=3.3.0
pymongo>=4.6.0

# Authentication & security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt 5.0+ removed __about__ which passlib 1.7.4 still reads at import time;
# the resulting AttributeError silently degrades password hashing to a path that
# rejects passwords > 72 bytes. Pin under 5.0 until passlib 1.8 ships.
bcrypt>=4.0.0,<5.0.0
cryptography>=41.0.0

# MFA
pyotp>=2.9.0

# HTTP clients
httpx>=0.25.0
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# YAML parsing
pyyaml>=6.0.0

# Optional KMS providers (lazy-imported in backend/encryption/kms.py)
boto3>=1.34.129

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
