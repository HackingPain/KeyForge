# experimental/

This directory holds prototype integrations that have a real shape but not yet a real implementation. Move a directory back into the project's main tree once it has tests, a working build pipeline, and an acceptance criterion documented in tasks/todo.md.
